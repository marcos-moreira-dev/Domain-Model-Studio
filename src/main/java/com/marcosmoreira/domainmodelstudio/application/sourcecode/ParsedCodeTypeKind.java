package com.marcosmoreira.domainmodelstudio.application.sourcecode;

/** Tipo estructural detectado dentro del código fuente. */
public enum ParsedCodeTypeKind {
    CLASS,
    INTERFACE,
    ENUM,
    RECORD,
    TYPE_ALIAS,
    UNKNOWN
}
