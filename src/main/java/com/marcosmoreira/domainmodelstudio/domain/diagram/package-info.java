/**
 * Núcleo del proyecto de diagrama y de sus documentos especializados.
 *
 * <p>Este paquete contiene el agregado general {@code DiagramProject}, la
 * metadata del proyecto, el modelo conceptual base y los identificadores de
 * elementos. Es la frontera común que permite guardar en un mismo archivo
 * {@code .dms} tipos visuales, documentales y matrices sin mezclar sus dominios.</p>
 *
 * <p>El modelo conceptual histórico se conserva aquí como parte del agregado,
 * pero los documentos especializados se agregan mediante campos explícitos para
 * evitar convertirlos en estructuras genéricas sin semántica.</p>
 */
package com.marcosmoreira.domainmodelstudio.domain.diagram;
